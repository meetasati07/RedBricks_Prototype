services:
  - type: web
    name: farmdirect-frontend
    env: static
    rootDir: frontend
    staticPublishPath: frontend/dist
    buildCommand: cd frontend && npm install && npm run build
    startCommand: echo "Static site - no start command needed"
    routes:
      - type: redirect
        source: /*
        destination: /index.html